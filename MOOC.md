# 中国大学 MOOC（控制台）

## 登录和执行

1. 在开启远程调试的专用浏览器中登录中国大学 MOOC，并打开课程页。
2. 默认使用本机 `9222` 端口；可通过 `YATORI_MOOC_CDP_URL` 指定其他本机 IP 调试地址。
3. 配置 `accountType: MOOC`。`videoModel: 0` 只查看课程目录；`videoModel: 1` 串行处理视频和文档。
4. `includeCourses` / `excludeCourses` 沿用现有课程名称筛选规则。

## 已支持的处理

- 视频：按实际经过的时间上报进度，支持读取续学位置。
- 文档：发送一次 `finished=true` 的文档回执，然后重新读取课程目录，只有该文档 `viewStatus=5` 才报告已确认。
- 服务端已标记的文档跳过写入。新文档上报之间至少间隔两秒；写入失败停止，不自动重复写入。
- 写入成功后最多读回两次状态。状态未更新则报错，避免把“接口返回成功”当成已学习。
- `Ctrl+C` 可取消等待和后续处理。

## 文档语义与范围

文档采用已实测的平台完成标记协议：第 1 页、`finished=true`、累计时间 0 秒。此流程不下载或解析 PDF，也不声称已经逐页阅读；无需增加 PDF 解析依赖。

“已学习”仅表示课程目录的资源标记。考试、作业、课程成绩以及其他类型任务尚未由本流程处理。

尚未取得明确原因的临时服务端拒绝会保留为错误，不能保证所有课程都接受相同文档协议。